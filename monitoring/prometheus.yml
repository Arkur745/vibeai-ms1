global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "/etc/prometheus/alerting_rules.yml"

scrape_configs:
  - job_name: "vibeai-microservice-metrics"
    static_configs:
      - targets:
          - "api:8000"